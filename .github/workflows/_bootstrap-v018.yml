name: Retired v0.18 Metadata Bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow
        run: |
          echo 'This one-off bootstrap is retired. v0.18 metadata is maintained explicitly in normal feature PRs.'
